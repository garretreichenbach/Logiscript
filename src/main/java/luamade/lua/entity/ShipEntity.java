package luamade.lua.entity;

import org.schema.game.common.controller.SegmentController;

/** @deprecated Use {@link Ship} instead. */
@Deprecated
public class ShipEntity extends Ship {
	public ShipEntity(SegmentController controller) {
		super(controller);
	}
}

