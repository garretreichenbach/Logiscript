# UsableChamber API

`UsableChamber` extends `Chamber` with activation controls for chambers that can be actively triggered.

## Reference

- `activate()`
Activates the chamber effect.

- `deactivate()`
Deactivates the chamber effect.

- `getCharge()`
Returns the current charge level as a `Double` (0.0–1.0).
